<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <title>{{ article.titre }}</title>
    <style>
      @page {
          margin: 0;
      }

      body {
          font-family: Arial, sans-serif;
          background-color: #1f2937;
          color: #f9fafb;
          margin: 0;
          padding: 40px;
      }

      h1 {
          color: #c084fc;
          font-size: 28px;
          margin-bottom: 10px;
      }

      p {
          margin-bottom: 12px;
          line-height: 1.6;
      }

      .label {
          font-weight: bold;
          color: #a1a1aa;
      }

      .divider {
          border-top: 1px solid #374151;
          margin: 30px 0;
      }

      .footer {
          font-size: 12px;
          color: #9ca3af;
          text-align: center;
          margin-top: 40px;
      }
  </style>
</head>
<body>

    <h1>{{ article.titre }}</h1>

    <p><span class="label">Artiste :</span> {{ article.artiste }}</p>
    <p><span class="label">Date :</span> {{ article.datePublication|date('d/m/Y') }}</p>

    <div class="divider"></div>

    <p>{{ article.contenu|nl2br }}</p>

    <div class="divider"></div>

    <div class="footer">
        Ce document a été généré automatiquement.
    </div>

</body>
</html>